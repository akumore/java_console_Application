= form_for @brick, :url => url do |f|
  = render 'cms/real_estates/errors', :model => @brick

  %fieldset
    .control-group
      = f.label :text
      .controls
        = f.text_area :text, :class => 'span8'
    .control-group
      = f.label :more_text
      .controls
        = f.text_area :more_text, :class => 'span8'

  .form-actions
    = f.submit nil, :class => 'btn btn-primary'
