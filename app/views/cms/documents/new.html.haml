= render 'wizard_header'

= render_navigation :context => :real_estate

= form_for @document, :as=>:document, :url=>cms_real_estate_documents_path(@real_estate), :html => { :multipart => true } do |f|
  = render 'form', :f=>f