= render 'wizard_header'

= render_navigation :context => :real_estate

= form_for @video, :as=>:video, :url=>cms_real_estate_video_path(@real_estate, @video), :method=>:put, :html => {:multipart => true } do |f|
  = render 'form', :f=>f