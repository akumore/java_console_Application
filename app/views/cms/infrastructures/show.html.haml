= render 'wizard_header'

= render_navigation :context => :real_estate

- if @infrastructure.nil?
  Für diese Immobilie wurde keine #{t "navigation.cms.real_estates_navigation.infrastructure"} hinterlegt.
- else
  %legend= t('.parking')

  %dl.dl-horizontal.extra-wide
    %dt= t "mongoid.attributes.infrastructure.has_parking_spot"
    %dd= t "#{@infrastructure.has_parking_spot?}"

    %dt= t "mongoid.attributes.infrastructure.has_roofed_parking_spot"
    %dd= t "#{@infrastructure.has_roofed_parking_spot?}"

    %dt= t "mongoid.attributes.infrastructure.has_garage"
    %dd= t "#{@infrastructure.has_garage?}"

    %dt= t "mongoid.attributes.infrastructure.inside_parking_spots"
    %dd
      = @infrastructure.inside_parking_spots
      &nbsp;

    %dt= t "mongoid.attributes.infrastructure.outside_parking_spots"
    %dd
      = @infrastructure.outside_parking_spots
      &nbsp;

    %dt= t "mongoid.attributes.infrastructure.covered_slot"
    %dd
      = @infrastructure.covered_slot
      &nbsp;

    %dt= t "mongoid.attributes.infrastructure.covered_bike"
    %dd
      = @infrastructure.covered_bike
      &nbsp;

    %dt= t "mongoid.attributes.infrastructure.outdoor_bike"
    %dd
      = @infrastructure.outdoor_bike
      &nbsp;

    %dt= t "mongoid.attributes.infrastructure.single_garage"
    %dd
      = @infrastructure.single_garage
      &nbsp;

    %dt= t "mongoid.attributes.infrastructure.double_garage"
    %dd
      = @infrastructure.double_garage
      &nbsp;

  %br
  %legend Distanzen

  %dl.dl-horizontal.extra-wide
    - @infrastructure.points_of_interest.each do |poi|
      %dt= t ".points_of_interest.types.#{poi.name}"
      %dd
        = poi.distance
        &nbsp;
