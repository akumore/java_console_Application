= render 'wizard_header'

= render_navigation :context => :real_estate

-if @additional_description.nil?
  Für diese Immobilie wurden keine #{t "navigation.cms.real_estates_navigation.additional_description"} hinterlegt.
-else
  %dl.dl-horizontal

    %dt= t "mongoid.attributes.additional_description.orientation_degrees"
    %dd
      = @additional_description.orientation_degrees
      &nbsp;
