.chapter.floorplan.pbi-avoid
  %h2= t('.title')

  .floorplan-image.pbi-avoid
    %div
      = @real_estate.floor_plans.first.north_arrow_img_tag
    = image_tag(@real_estate.floor_plans.first.file.minidoku.url)
