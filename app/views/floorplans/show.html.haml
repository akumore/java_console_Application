- if params[:print].present?
  :coffeescript
    window.onload = ->
      window.print()

.floorplan
  .north-arrow
    = @floor_plan.north_arrow_img_tag
  = image_tag @floor_plan.file.gallery_zoom.url

